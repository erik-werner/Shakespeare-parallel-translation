# Shakespeare-parallel-translation
Shakespeare's plays, with the original text set in parallel with the Swedish translation of C. A. Hagberg

## To install

```bash
pip install -r requirements.txt
```
